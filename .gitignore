node_modules/
.vercel/
.DS_Store

.vercel

# Variables de entorno locales — nunca commitear la key real.
# (.env.example SÍ se commitea; solo documenta los nombres.)
.env
.env*.local

# Salida del grafo de conocimiento (graphify) — herramienta de análisis, no es parte del sitio.
graphify-out/

# Trabajo en curso, fuera del sitio. Está acá para que un `git add -A` no lo
# arrastre a un commit sin querer.
certificado.html

# Config local de Claude Code (permisos, servidores de preview). Es de la
# máquina, no del proyecto.
.claude/
